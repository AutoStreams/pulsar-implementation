name: Deploy Javadoc

on:
  push:
    branches: [ master, feature/javadoc]
  pull_request:
    branches: [ master ]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy JavaDoc 🚀
        uses: AutoStreams/jvpub@e5013287ddcca8926a34d40ef83827ae9815e6b7
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          javadoc-branch: gh-pages 
          java-version: 17
